use second::Solution;

fn main() {
    // println!("Hello, world!");
    let a: u8 = 1.to_string().as_bytes()[0];
    let b: u8 = 0.to_string().as_bytes()[0];
    let c: u8 = 2.to_string().as_bytes()[0];
    Solution::add_binary(1.to_string(),
                      11.to_string());
    // println!(a);
}
